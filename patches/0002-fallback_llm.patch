From: mechanic-Q
Date: 2026-05-08
Subject: [PATCH] FallbackLLM — None guard + provider blacklist

This file is in-repo (fallback_llm.py), not in site-packages.
The patch is for reference only — the actual file lives in .mem0-server/.
If fallback_llm.py gets overwritten (git reset, re-clone, etc.),
restore from the repo itself (it's version-controlled).

Changes:
1. Added None response guard (L71-77 in current version)
2. Added persistent provider blacklist (JSON file, provider_blacklist.json)
3. Quota errors (402/quota/insufficient/billing) auto-blacklist providers
4. Blacklisted providers are skipped on subsequent calls instead of wasting timeouts

Full file: see fallback_llm.py in repo root.
